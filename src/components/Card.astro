---
interface Props {
	title: string;
	body: string;
	href: string;
}

const { href, title, body } = Astro.props;
---

<!-- <li class="link-card">
	<a href={href}>
		<h2>
			{title}
			<span>&rarr;</span>
		</h2>
		<p>
			{body}
		</p>
	</a>
</li> -->
<li class="card">
	<a href="#">
		<div class="header">
			<div class="header-container">
				<img src="https://nuxt.com/assets/blog/v3.6.jpg" alt="">
			</div>
		</div>
		<div class="body">
			<div class="body-container">
				<div class="title">
					<h4>第1期</h4>
					<span>收到发生的</span>
				</div>
				<p>测试中文文字测试中文文字测试中文文字测试中文文字</p>
			</div>
		</div>
		<div class="footer">
			<div class="footer-container">
				<span class="time">2023年8月11日</span>
			</div>
		</div>
	</a>
</li>
<style>
	.card{
		display: block;
		border: 1px solid #eee;
		position: relative;
		border-radius: .75rem;
		transition: all .2s;
		overflow: hidden;
	}
	.card:hover{
		box-shadow: 0 0 0 1px var(--green-500);
		border-color: var(--green-500);
	}
	.card:hover .body-container h4{
		background-color: var(--green-400);
	}
	.card a{
		display: flex;
		height: 100%;
		flex-direction: column;
		justify-content: space-between;
	}
	.card .header-container img{
		width: 100%;
		height: 150px;
		object-fit: cover;
	}
	.card .body-container{
		padding: 1rem;
	}
	.card .title{
		display: flex;
		align-items: center;
	}
	.card .body-container h4{
		font-size: .8rem;
		margin-right: 10px;
		background-color: #fff;
		color: #333;
		padding: 0 10px;
		border-radius: 30px;
		transition: all .2s;
	}
	.card .body-container span{
		font-size: 1.2rem;
	}
	.card .body-container p{
		font-size: 1rem;
	}
	.card .footer-container{
		display: flex;
		padding: .6rem 1rem;
	}
	.card .time{
		color: #999999;
		font-size: .9rem;
	}
	.link-card {
		list-style: none;
		display: flex;
		padding: 1px;
		background-color: #23262d;
		background-image: none;
		background-size: 400%;
		border-radius: 7px;
		background-position: 100%;
		transition: background-position 0.6s cubic-bezier(0.22, 1, 0.36, 1);
		box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.1);
	}
	.link-card > a {
		width: 100%;
		text-decoration: none;
		line-height: 1.4;
		padding: calc(1.5rem - 1px);
		border-radius: 8px;
		color: white;
		background-color: #23262d;
		opacity: 0.8;
	}
	h2 {
		margin: 0;
		font-size: 1.25rem;
		transition: color 0.6s cubic-bezier(0.22, 1, 0.36, 1);
	}
	p {
		margin-top: 0.5rem;
		margin-bottom: 0;
	}
	.link-card:is(:hover, :focus-within) {
		background-position: 0;
		background-image: var(--accent-gradient);
	}
	.link-card:is(:hover, :focus-within) h2 {
		color: rgb(var(--accent-light));
	}
</style>
